import { styled } from "styled-components";

export const Container = styled.div`
  .page-title {
    margin-bottom: 20px;
  }

  .card-list {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
  }
`;
